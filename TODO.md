# TODO

Use this file to collect future work items.

- Check how the game series system is working in the backend. Make sure that deleting a series does not allow it to be accidentally created again, and that deleting one game from a series does not allow that game to be recreated.
- Update modals so they do not show a close button and instead close when clicking the backdrop.
- Replace window alerts/confirms with app modals where they are currently used.
